# -*- makefile -*-

$(LIBGCOV): $(OBJ_LIBGCOV)
	$(AR_MESSAGE)
	$(VERBOSE)$(RM) $@
	$(VERBOSE)$(AR) $(ARFLAGS) $@ $^

clean-LIBGCOV:
	$(VERBOSE)$(RM) *.gcno

fiasco-covpath.o:
	$(COMP_MESSAGE)
	$(VERBOSE)echo 'char const *__COV_PATH="$(PWD)/fiasco";' | $(CC) -o $@ -c -xc -
