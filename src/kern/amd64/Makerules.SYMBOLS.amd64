# -*- makefile -*-

$(SYMBOLS):	$(KERNEL).debug
		$(COMP_MESSAGE)
		$(VERBOSE)$(NM) -nC $(KERNEL).debug|grep "^f[0-9a-z]\{15,15\}">$@.new
		$(VERBOSE)mv $@.new $@
		$(VERBOSE)chmod 644 $@

clean-SYMBOLS:
		$(VERBOSE)$(RM) $(SYMBOLS)
