# -*- makefile -*-

$(SYMBOLS):	$(KERNEL).debug
		$(COMP_MESSAGE)
		$(VERBOSE)$(NM) -nC $(KERNEL).debug | \
		   sed -e 's/^00000000//' | grep "^f[0-9a-z]\{7,7\}">$@.new
		$(VERBOSE)mv $@.new $@
		$(VERBOSE)chmod 644 $@

clean-SYMBOLS:
		$(VERBOSE)$(RM) $(SYMBOLS)
